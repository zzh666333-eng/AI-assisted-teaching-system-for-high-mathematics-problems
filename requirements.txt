# --- Google Gemini API & Vertex AI ---
# 核心 SDK，用于调用 Gemini 2.5 Pro 和多模态功能
google-genai>=0.3.0

# --- 数学与语义去重 (PostProcessor 依赖) ---
# 用于语义去重引擎的模型加载
sentence-transformers>=3.0.0
# 用于余弦相似度计算
scikit-learn>=1.3.0
# 基础数值计算
numpy>=1.24.0
# 深度学习框架 (用于 CUDA/GPU 加速，建议由组员根据环境自行微调)
torch>=2.0.0

# --- 进度管理与可视化 (UserSession 依赖) ---
# 用于生成学习情况分析图表
matplotlib>=3.7.0

# --- 系统配置与环境 ---
# 用于读取 .env 文件中的 PROJECT_ID 和 LOCATION
python-dotenv>=1.0.1
# 异步网络请求支持
httpx>=0.27.0